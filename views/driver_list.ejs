<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>ドライバーユニット一覧</title>
</head>
<body>
    <h1>ドライバーユニット一覧</h1>
    <a href="/driver/create">新規登録</a>
    <hr>
    <table border="1">
        <tr>
            <th>ID</th>
            <th>型番(Code)</th>
            <th>名前</th>
            <th>操作</th>
        </tr>
        <% for (let item of data) { %>
        <tr>
            <td><%= item.id %></td>
            <td><%= item.code %></td>
            <td><%= item.name %></td>
            <td>
                <a href="/driver/<%= item.id %>">詳細</a> | 
                <a href="/driver/edit/<%= item.id %>">編集</a> | 
                <a href="/driver/delete/<%= item.id %>" onclick="return confirm('本当に削除しますか？');">削除</a>
            </td>
        </tr>
        <% } %>
    </table>
</body>
</html>