<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>HMDリスト</title>
</head>
<body>
    <h1>VR HMD リスト</h1>
    <a href="/hmd/create">新規登録</a>
    <hr>
    <table border="1">
        <tr>
            <th>ID</th>
            <th>製品名</th>
            <th>メーカー</th>
            <th>価格</th>
            <th>操作</th>
        </tr>
        <% for (let item of data) { %>
        <tr>
            <td><%= item.id %></td>
            <td><%= item.name %></td>
            <td><%= item.maker %></td>
            <td><%= item.price %>円</td>
            <td>
                <a href="/hmd/<%= item.id %>">詳細</a> | 
                <a href="/hmd/edit/<%= item.id %>">編集</a> | 
                <a href="/hmd/delete/<%= item.id %>" onclick="return confirm('削除しますか？');">削除</a>
            </td>
        </tr>
        <% } %>
    </table>
</body>
</html>